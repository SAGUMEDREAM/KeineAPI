package cc.thonly.keine.loot;

import net.minecraft.core.Holder;
import net.minecraft.world.level.storage.loot.LootTable;

public interface KeineLootTable {
   void keine$setHolder(Holder<LootTable> holder);
}
